import { ParametarProceneZem } from './parametar-procene-zem';

describe('ParametarProceneZem', () => {
  it('should create an instance', () => {
    expect(new ParametarProceneZem()).toBeTruthy();
  });
});
